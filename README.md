# yt-dlp-ext

Chrome extension for downloading YouTube audio directly from the browser. Bypasses POT by running a content script on YouTube's origin via an embedded iframe.

## Development

```bash
pnpm i

# Start dev rebuild watch, then load dist/ext-dev as unpacked extension in Chrome
pnpm dev
```
